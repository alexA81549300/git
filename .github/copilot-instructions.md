# Hermes Copilot Instructions: git

Treat `alexA81549300/git` as an owned public `REFERENCE_MIRROR` fork of `git/git`, not as a Hermes runtime organ.

## Authority boundary

- GitHub is reference/evidence surface only.
- Runtime authority: `NO`.
- Canon authority: `NO`.
- Historical donor/vendor role: `UNKNOWN_TYPED` until exact lineage proves it.
- Do not infer role from public visibility, ownership, repository name, or fork status.

## Worker job

This worker is audit-only. Preserve upstream Git source behavior. Hermes role-closure work may update only Hermes-specific metadata unless an explicit, separate source-code workorder is issued.

Allowed work:
- verify parent/source and exact refs;
- compare sanitized metadata or Git identities;
- find repo-specific Hermes lineage in receipts, role manifests, vendor locks, submodule history, or DNA summaries;
- report contradictions and explicit unknowns.

Forbidden work:
- no runtime truth claims from GitHub;
- no raw `.mesh`, secrets, dumps, prompts, payloads, or archive contents;
- no source rewrite to make the repo look more Hermes-like;
- no rename/delete/prune/reset/rebase/force as metadata closure;
- no claim that this repo is a vendor tool or donor bank without exact evidence.

Every output must separate `PROVEN_CURRENT`, `PROVEN_HISTORICAL`, and `UNKNOWN_TYPED`.
